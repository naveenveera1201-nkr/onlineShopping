spring:
  application:
    name: nktprocess
  mongodb:
#      uri: mongodb://localhost:27017/nktdb
      uri: mongodb+srv://nammakadaitheru_db_user:Nkt_User_01@nkt-cluster.1trwdu1.mongodb.net/nktdb?retryWrites=true&w=majority&appName=nkt-cluster
      database: nktdb
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8070
  servlet:
    context-path: /

# ── JWT ───────────────────────────────────────────────────────────────────────
jwt:
  secret: bm10LXNlY3JldC1rZXktZm9yLW5hbW1ha2FkYWl0aGVydS1hcGktMjAyNA==
  expiration: 9000000000
  refresh-expiration: 259200000000
otp:
  expiration: 30     # 3 mins
  dummy: Y
image:
  basepath: ${IMAGE_BASEPATH:https://nkt-process-service.onrender.com/images/store/}
bannerimage:
  basepath: ${BANNER_IMAGE_BASEPATH:classpath*:/assets/images/store/}

# ── Logging ───────────────────────────────────────────────────────────────────
logging:
  level:
    com.controller: DEBUG
    com.service: DEBUG
    com.security: INFO
    org.springframework.security: INFO

aws:
  accessKey: 123
  secretKey: 123
  region: 132
  s3:
    bucketName: 23QWW

# ── Firebase Cloud Messaging ─────────────────────────────────────────────────
# Local dev  : set FIREBASE_CREDENTIALS_PATH to the absolute path of your
#              service-account JSON (never commit that file).
# Production : set FIREBASE_CREDENTIALS_JSON to the service-account JSON,
#              base64-encoded, as a Render/host environment variable.
# Neither set: falls back to GOOGLE_APPLICATION_CREDENTIALS / ADC discovery.
# See docs/FCM_INTEGRATION.md for full setup instructions.
firebase:
  enabled: ${FIREBASE_ENABLED:false}
  project-id: ${FIREBASE_PROJECT_ID:}
  credentials-path: ${FIREBASE_CREDENTIALS_PATH:}
  credentials-json: ${FIREBASE_CREDENTIALS_JSON:}
