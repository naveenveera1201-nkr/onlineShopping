spring:
  config:
    import: classpath:config-master.yml
  application:
    name: myFirstApplication
  mongodb:
    uri: mongodb+srv://nammakadaitheru_db_user:Nkt_User_01@nkt-cluster.1trwdu1.mongodb.net/nktdb?retryWrites=true&w=majority&appName=nkt-cluster
    database: nktdb

server:
  port: 8060

# ── JWT ───────────────────────────────────────────────────────────────────────
jwt:
  secret: bm10LXNlY3JldC1rZXktZm9yLW5hbW1ha2FkYWl0aGVydS1hcGktMjAyNA==
  expiration: 900000          # 15 min in ms
  refresh-expiration: 2592000000  # 30 days in ms
otp:
  expiration: 3     # 3 mins

# ── Downstream services ───────────────────────────────────────────────────────
services:
  nkt-process:
    url: http://localhost:8070
