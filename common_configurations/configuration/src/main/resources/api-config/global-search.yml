apis:
  - id: global-search
    name: Global Search
    version: v1
    method: GET
    path: /api/v1/search
    description: Full-text search across stores and products. Returns ranked results with category context.
    enabled: true
    security:
      authentication:
        enabled: true
        type: JWT
        roles:
        - customer
      authorization:
        enabled: true
      rateLimit:
        enabled: true
        requestsPerMinute: 60
        requestsPerHour: 1000
      encryption:
        enabled: true
        algorithm: AES256
    request:
      contentType: application/json
      parameters:
      - name: q
        type: String
        location: BODY
        required: true
        validation:
          minLength: 1
          maxLength: 200
          errorMessage: Search query must be at least 1 character
      - name: latitude
        type: Double
        location: BODY
        required: false
        validation:
          min: -90.0
          max: 90.0
          errorMessage: Latitude must be between -90 and 90
      - name: longitude
        type: Double
        location: BODY
        required: false
        validation:
          min: -180.0
          max: 180.0
          errorMessage: Longitude must be between -180 and 180
      - name: radiusKm
        type: Double
        location: BODY
        required: false
        validation:
          min: 0.1
          max: 500.0
          errorMessage: Radius must be greater than 0
      - name: categoryId
        type: String
        location: BODY
        required: false
        validation:
          minLength: 1
          errorMessage: Category ID must be a valid string
      - name: storeId
        type: String
        location: BODY
        required: false
        validation:
          minLength: 1
          errorMessage: Store ID must be a valid string
    response:
      successCode: 200
      contentType: application/json
      fields:
      - name: id
        type: Long
        description: SearchResult identifier
      - name: history
        type: String
        description: history
      - name: data
        type: String
        description: response data
      - name: currentStatus
        type: String
        description: Current record status
        defaultValue: ACTIVE
      - name: fetchedAt
        type: LocalDateTime
        description: Data retrieval timestamp
    businessLogic:
      businessLogicRequired: "Y"
      processCode: nkt.location.global_search
      type: CUSTOM_SERVICE
      dataSource: primaryDb
      operations:
      - type: SELECT
        entity: SearchResult
        mapping:
          q: "#{request.q}"
          latitude: "#{request.latitude}"
          longitude: "#{request.longitude}"
          categoryId: "#{request.categoryId}"