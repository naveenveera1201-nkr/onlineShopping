apis:
  - id: register-fcm-token
    name: Register Token
    version: v1
    method: POST
    path: /api/v1/fcm/token/register
    description: Register fcm token against the mobile users
    enabled: true
    security:
      authentication:
        enabled: true
        type: JWT
        roles:
        - customer
        - business
        - employee
      authorization:
        enabled: false
      rateLimit:
        enabled: true
        requestsPerMinute: 10
        requestsPerHour: 100
      encryption:
        enabled: true
        algorithm: AES256
    request:
      contentType: application/json
      parameters:
      - name: storeId
        type: String
        location: BODY
        required: true
      - name: userType
        type: String
        location: BODY
        required: true
      - name: deviceId
        type: String
        location: BODY
        required: true
      - name: fcmToken
        type: String
        location: BODY
        required: true
      - name: platform
        type: String
        location: BODY
        required: true
    response:
      successCode: 200
      contentType: application/json
      fields:
      - name: id
        type: Long
        description: Generated AuthToken ID
      - name: history
        type: String
        description: history
      - name: data
        type: String
        description: registered data
      - name: currentStatus
        type: String
        description: Record status
        defaultValue: ACTIVE
      - name: createdAt
        type: LocalDateTime
        description: Creation timestamp
      - name: createdBy
        type: String
        description: created by
    businessLogic:
      businessLogicRequired: "Y"
      processCode: nkt.notification.fcm.register_device
      type: CUSTOM_SERVICE
      dataSource: primaryDb
      operations:
      - type: INSERT
        entity: AuthToken
        mapping:
          identifier: "#{request.identifier}"
          otp: "#{request.otp}"
          userType: "#{request.userType}"
          purpose: "#{request.purpose}"
          name: "#{request.name}"
          email: "#{request.email}"
          createdAt: "#{now()}"
          status: ACTIVE
    callbacks:
    - event: SUCCESS
      type: WEBHOOK
      url: "https://api.nammakadaitheru.in/webhooks/authtoken-event"
